//! The UniFFI bindgen CLI entrypoint. Foreign-language bindings (Kotlin/Swift)
//! are generated by running this binary against the built library, so no `.udl`
//! file is needed — the scaffolding is produced by the proc-macros in the lib.
fn main() {
    uniffi::uniffi_bindgen_main()
}
